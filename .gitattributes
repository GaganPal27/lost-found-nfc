# Ensure all TypeScript/JS source files always use LF on checkout
# (prevents CRLF from breaking Metro bundler on Linux EAS builds)
*.ts   text eol=lf
*.tsx  text eol=lf
*.js   text eol=lf
*.jsx  text eol=lf
*.json text eol=lf
*.md   text eol=lf
